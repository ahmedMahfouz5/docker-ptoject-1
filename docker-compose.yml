version: "3.7.0"
services:
  node-app:
    container_name: api
    build: .
    ports:
      - 3000:3000
    env_file:
      - ./.env
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
